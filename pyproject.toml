[build-system]
requires = ["hatchling>=1.18", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "reset-bio"
##version = "0.1.0" DEPRECATED -> use versioning from VCS
dynamic = ["version"]
description = "Reference genome Selection Tool"
readme = "README_PYPI.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [
  { name = "Jasper van Bemmelen", email = "jaspervanbemmelen@hotmail.com" }
]
dependencies = [
    "numpy>=2.0,<3.0",
    "scipy>=1.14,<2.0"
]

classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
]

[project.urls]
Repository = "https://github.com/JaspervB-tud/ReSeT"
Changelog  = "https://github.com/JaspervB-tud/ReSeT/blob/main/CHANGELOG.md"

[project.optional-dependencies]
test = ["pytest>=8,<9"]

dashboard = [
  "streamlit>=1.37,<2.0",
  "pandas>=2.0,<3.0",
]

[tool.hatch.version]
source = "vcs"

# pytest initialization
[tool.pytest.ini_options]
pythonpath = ["src"]
addopts = "-p no:cacheprovider"

[tool.hatch.build.targets.wheel]
packages = ["src/reset"]
artifacts = ["src/reset/examples/**"]

[tool.hatch.build.targets.sdist]
include = [
    "src/reset",
    "README_PYPI.md", 
    "LICENSE", 
    "tests", 
]